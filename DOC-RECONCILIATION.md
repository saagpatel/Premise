# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass. The pass treats the code as read-only ground truth and edits only documentation so it reflects the repo's actual current state. No code was changed and no builds or tests were run. This file explains the findings to anyone who reads it later.

---

## Per-Claim Findings

### 1. What it is

**Status:** `consistent`
**Evidence:** `verified-by-reading-code`

README description matches the code: structured debate platform with typed argument nodes, anonymous cookie identity, crux detection, and spectator voting. All components, API routes, and types confirm the feature set.

---

### 2. Current state

**Status:** `drifted`
**Evidence:** `verified-by-reading-code`

CLAUDE.md (Portfolio Context → Current State) claimed "Phase 0: Foundation" as the current phase. The code shows all four phases are complete:
- Phase 0 artifacts: `supabase/seed.sql`, `types/index.ts`, `lib/supabase/client.ts`, `lib/supabase/server.ts`, `app/api/auth/anon-id/route.ts`, `app/api/health-check/route.ts`
- Phase 1 artifacts: `app/api/debates/route.ts`, `app/api/arguments/route.ts`, `lib/d3/tree-layout.ts`, `lib/crux-finder.ts`, all `components/debate/` files
- Phase 2 artifacts: `lib/supabase/realtime.ts`, `app/api/votes/route.ts`, `components/debate/voting-controls.tsx`, `components/debate/thread-view.tsx`
- Phase 3 artifacts: `app/(auth)/sign-in/page.tsx`, `app/(auth)/callback/route.ts`, `app/(public)/debate/[id]/page.tsx`, `components/debate/username-modal.tsx`
- Phase 4 artifacts: `app/api/classify-argument/route.ts`, `app/api/flags/route.ts`, `middleware.ts`, `lib/rate-limit.ts`, `DEPLOY.md`, `e2e/smoke.spec.ts`

`docs/PORTFOLIO-DISPOSITION.md` explicitly states "Phases 0-4 complete, launch-ready."

**Fixed in:** `CLAUDE.md`
Before → After: `"Phase 0: Foundation / See IMPLEMENTATION-ROADMAP.md → Phase 0…"` → `"Phases 0–4 complete — launch-ready. Foundation, core debate flow, real-time + voting, auth + discovery, and launch prep are all shipped."`

Also updated the "Next Recommended Move" section from "Finish Phase 0 tasks" to reflect the actual next step: confirm production Supabase posture + run Playwright E2E per DEPLOY.md.

---

### 3. Stack

**Status:** `drifted`
**Evidence:** `verified-by-reading-code`

Three drifts found:

**3a. Next.js version** (`CLAUDE.md` — two occurrences, fixed both)
- `package.json:16` — `"next": "15.5.15"` (Next.js 15, not 14)
- CLAUDE.md header line and Stack section said "Next.js 14" / "Next.js 14.2+"
- Fixed in `CLAUDE.md`: both occurrences changed to "Next.js 15" / "Next.js 15.5+"

**3b. D3 layout described as "force-directed"** (`README.md`)
- `lib/d3/tree-layout.ts:65` — `d3.tree<Argument>().nodeSize([60, 220])` — explicitly uses `d3.tree()`, not force-directed
- CLAUDE.md's Scoped Gates section even prohibits force-directed: "Force-directed layout thrashes during live node inserts"
- README Features bullet said "interactive force-directed graph"
- Fixed in `README.md`: changed to "interactive tree (d3.tree() Reingold-Tilford layout)"

**3c. AI classifier described as requiring Anthropic key** (`README.md`)
- `lib/classify.ts:29-71` — full Ollama fallback implemented; `lib/classify.ts:144-159` — classifyArgument() tries Anthropic first, then Ollama, then returns null gracefully
- `.env.example:13-14` — documents OLLAMA_URL and OLLAMA_MODEL
- README said "requires Anthropic API key"
- Fixed in `README.md`: clarified that it uses Anthropic if key is set, falls back to Ollama if available, or degrades gracefully

---

### 4. How to run

**Status:** `drifted`
**Evidence:** `verified-by-reading-code`

Three drifts found:

**4a. npm vs pnpm** (`README.md` Quick Start — two occurrences, fixed both)
- `package.json:6` — `"packageManager": "pnpm@9.15.9"`
- README Quick Start used `npm install` and `npm run dev`
- Fixed in `README.md`: changed to `pnpm install` and `pnpm dev`

**4b. npm vs pnpm in Portfolio Context** (`CLAUDE.md`)
- CLAUDE.md Portfolio Context → How To Run used `npm run dev`
- Fixed in `CLAUDE.md`: changed to `pnpm dev`

**4c. Missing test script** (`CLAUDE.md`)
- `package.json:7-11` — scripts are: `dev`, `build`, `start`, `lint`, `type-check`; there is NO `test` script
- CLAUDE.md Build/Test/Run said `pnpm test # unit tests (Vitest)`; this command would fail — pnpm has no `test` entry to forward to
- Vitest IS configured (`vitest.config.ts` exists, `vitest` in devDependencies, two `.test.ts` files)
- Fixed in `CLAUDE.md`: changed to `pnpm exec vitest run  # unit tests (Vitest — no "test" script in package.json)`

---

### 5. Known risks

**Status:** `consistent` (in CLAUDE.md Scoped Gates); `unverifiable` (README has no risks section)
**Evidence:** `verified-by-reading-code`

CLAUDE.md's risk list accurately reflects real code constraints: service role key used only in `lib/supabase/server.ts`; d3.tree() enforced in `lib/d3/tree-layout.ts`; AI classifier non-blocking in `app/api/classify-argument/route.ts`. No contradictions found.

**Note (not a doc drift — code-level observation):** The `.env.example` documents 5 Sentry-related env vars (`NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_AUTH_TOKEN`) and `NEXT_PUBLIC_APP_URL` that are not mentioned in README.md or CLAUDE.md. `@sentry/nextjs@10.52.0` is a production dependency. This is an omission rather than a contradiction; noted here for operator awareness.

---

### 6. Next move

**Status:** `drifted` (fixed as part of Claim 2 above)
**Evidence:** `verified-by-reading-code`

See Claim 2 — the "Next Recommended Move" section pointed at Phase 0 work that is complete. Updated in `CLAUDE.md`.

---

## Contradictions for Manual Review

These items are in files outside the allowed edit list. A human should make the following fixes:

| File | Location | What is wrong | One-line fix |
|------|----------|---------------|--------------|
| `IMPLEMENTATION-ROADMAP.md` | Dependencies section (line ~422) | Uses `npx create-next-app@14` and `npm install` throughout — project is Next.js 15 and uses pnpm | Replace `create-next-app@14` with `create-next-app@latest` and `npm install` with `pnpm install` throughout this section |
| `IMPLEMENTATION-ROADMAP.md` | Phase 0 Task 1 verification (line ~533) | `npm run dev` in verification checklist | Change to `pnpm dev` |
| `SETUP.md` | Step 2 env var table (line ~31) | Missing `NEXT_PUBLIC_APP_URL`, `OLLAMA_URL`, `OLLAMA_MODEL`, and Sentry vars that appear in `.env.example` | Add a second table for optional/monitoring vars covering those five `.env.example` entries |
| `DEPLOY.md` | Step 1 Supabase Setup (line ~12) | Lists `debates` table for Realtime enablement, but `SETUP.md` only lists `arguments` and `votes` — the `debates` table inclusion in DEPLOY.md may or may not be intentional | Verify whether Realtime on `debates` is required; if not, remove from DEPLOY.md; if yes, add to SETUP.md |
| `app/(public)/page.tsx` | `INSTALL_COMMANDS` constant (line ~290) | Source code string shown to visitors in "Self-Host in 10 Minutes" uses `npm install` and `npm run dev` | Change to `pnpm install` and `pnpm dev` (this is source code, not editable here, but affects user-visible text) |

---

## Footer

**Generated:** 2026-06-02 19:50:10 PDT
**Branch:** `docs/truth-up-2026-06-02`
**HEAD reconciled against:** `ef977a7b24b570c500a4ef24f5d7f39071468836`
